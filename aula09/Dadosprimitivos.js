/**
 * Dados primitivos: string, number
 */

//Exemplos:

const nome = "Thiago"       //String
const num1 =10              //Number
const num = 10.52           //Number
let nomeAluno               //Declara a variavel e inicializa com 'undefined'
let sobreNomeAluno= null    //Inicializa com nulo, não apontando para nenhum lugar da memoria
const aprovado=true         //Boolean

console.log(`Tipo da variavel: ${typeof nome}`)
console.log(`Tipo da variavel: ${ typeof num1}`)
console.log(`Tipo da variavel: ${typeof aprovado}`)